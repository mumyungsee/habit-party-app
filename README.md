# 습관파티 — 팀으로 함께하는 프로젝트 완주 앱

혼자 하는 습관은 작심삼일이지만, 같이 하면 끝까지 갑니다.

작은 파티로 모여 매일 자기 프로젝트의 한 걸음을 체크하고, 같이 보는 대시보드로 서로를 응원하는 실행 인증 웹앱입니다. "혼자 가면 지치는 길을 친구들과 함께"라는 생각에서 출발했습니다.

## 어떻게 동작하나요

- **팀(파티) 단위 미션** — 소규모 파티로 묶여 매일 자기 미션을 인증합니다.
- **같이 보는 대시보드** — 누가 했는지 한눈에 보이고, 절반부터 불꽃·3분의 2부터 빨간 불꽃이 켜집니다.
- **응원의 순간** — 인증할 때마다 작은 축하가, 팀 전원 완주엔 여러 불꽃과 큰 폭죽이 터집니다.
- **가입 없이 시작** — 이름 선택 + 핀 4자리면 끝. 무거운 회원가입 없이 바로.
- **홈 화면 앱 설치(PWA)** — 카톡 링크로 들어와 폰 홈 화면에 앱처럼 설치할 수 있습니다.

## 만든 이야기

`추석 전, AI로 끝내는 내 프로젝트`를 위해 기존 습관파티 MVP를 다듬었습니다. 전체 챌린지는 9월 5~23일 19일이고, 앱 실행 인증은 준비 기간을 뺀 9월 7~23일 17일입니다. 거창한 기능보다 "팀이 매일 체크하고 같이 본다"는 한 가지에 집중했습니다.

<!-- ──────────────────────────────────────────────
     아래는 개발자용 메모 (사이트엔 안 보임)
     ────────────────────────────────────────────── -->

## 구조 (개발자용)
- `index.html` / `style.css` / `app.js` — 화면·로직
- `data.js` — 데이터 레이어 (구글시트 Apps Script 연동)
- `apps-script/Code.gs` — 백엔드 (구글시트에 붙는 Apps Script, 배포 코드)
- `apps-script/셋업가이드.md` — 시트·배포 셋업 방법

## 데이터
구글시트 = DB. 시트 2탭:
- `members`: id|name|team|role|mission|emoji|pin (사람들이 자기 역할 줄에 이름 적음)
- `checkins`: memberId|name|day|date|done|memo|updatedAt (인증 시 자동 적재)

## 신분
이름 선택 + 핀 4자리 (처음 설정, 시트 저장). 가입·이메일 없음.

## 로컬 테스트
```
python -m http.server 8123
# → http://localhost:8123
```
(file:// 로 열면 CORS로 시트 연동 막힘 → 서버나 호스팅 필요)

데이터·백엔드 경계 테스트:
```
node scripts/verify.mjs
```

이 명령은 구문·전체 자동 검사·운영/QA 생성물 일치·날짜/캐시/업데이트 경계를 순차 검사한다. 실제 브라우저 검수나 Google 저장 검증을 대신하지 않으며 배포하지 않는다. 배포 전에는 아래 브라우저 검수와 별도 Google 테스트 서버의 새 버전 검증을 추가로 수행한다. 실제 기기·PWA 미검증은 별도 표시한다.

안전 수정의 배포 순서는 새 프런트엔드 → 검증된 서버 버전이다. 새 클라이언트의 expectedDay는 기존 서버가 무시하므로 이전 서버와 호환된다. 새 서버는 날짜 없는 구버전의 완료는 허용하되 취소는 거절한다(구버전 사용자는 새로고침 필요). 서버도 갱신하기 전까지 날짜 경계 보호가 완료됐다고 보고하지 않는다. 변경 중 운영 데이터 초기화는 하지 않는다.

실제 HTML/JS와 서버 코드로 가상 참가자 전체 흐름 검수:

```
node tests/browser-server.cjs
```

`http://127.0.0.1:8769/runner`에서 검수 실행을 누른다. Google 서비스만 메모리 속 가상 시트로 대체하며, 앱 API 주소는 이 로컬 서버에서만 바뀐다. 운영 API로 쓰기 요청을 보내지 않는다. 로그인·저장·응답 유실·자정·종료·다른 기기 상태·실제 15초 시간 초과를 순차 검증한다. 끝나면 서버를 종료한다. 실제 Google 호스팅의 쓰기 통신이나 실물 iPhone/PWA 설치 검증을 대신하지 않는다.

## 외부 기기 테스트

인증 버튼은 체크·파티 불꽃·팡파레를 즉시 미리 표시하고 `저장 중…`을 붙인다. 서버 응답 후에만 `오늘 인증 완료`로 확정한다. 실패하면 미리 표시한 체크를 되돌리고 서버 기록을 다시 읽는다. 결과를 확인할 수 없으면 안내와 읽기 전용 재확인 버튼을 남기며 쓰기 요청은 자동 반복하지 않는다. 같은 서울 날짜에는 사전 전체 조회 없이 저장하며, 날짜가 바뀐 경우에만 먼저 일정을 갱신한다. 가상 브라우저 검수에는 3초 지연 시 즉시 반응·중복 팡파레 방지·실패 복구가 포함된다.

운영 도구는 `qa/index.html`, 동일 코드의 테스트 앱은 `qa/app/`다. 테스트01은 운영자 수동 점검용(처음 PIN 직접 설정), 테스트02는 자동 연결 점검용이다. 운영 명단과 섞이지 않는다.

참가자 코드 수정 후 `node scripts/build-external-qa.mjs <테스트_API_URL>`로 테스트 화면을 다시 생성한다. root app.js/style.css를 그대로 사용하며 데이터 주소·로그인 키·서비스워커 캐시·PWA 설치 식별자만 테스트용으로 분리한다. 생성 후 `node --test tests/qa-*.test.cjs`와 실제 Google 테스트 서버 점검을 통과한 버전을 배포한다. 운영 앱 파일은 이 생성 단계에서 바꾸지 않는다.

테스트 서버 배포는 `scripts/deploy-external-qa.mjs --publish`가 기존 테스트 프로젝트에만 수행한다. 실행마다 새 버전/배포가 만들어지므로 정상 URL이 있으면 반복 실행하지 않는다. 운영 서버는 대상에 포함되지 않는다. 새 Apps Script 첫 실행의 Google 소유자 권한 승인이 필요할 수 있다.

## 배포
GitHub Pages — https://mumyungsee.github.io/habit-party-app/ (레포 mumyungsee/habit-party-app)

## 관련
- 기획·작업 기록: `../../docs/devlog/`
- 프로젝트 맥락: 메모리 `project_habit-party-launch`
